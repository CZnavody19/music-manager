input LoginInput {
    username: String!
    password: String!
}

extend type Mutation {
    login(input: LoginInput!): String!
    logout: Boolean! @auth
    changeLogin(input: LoginInput!): Boolean! @auth
}