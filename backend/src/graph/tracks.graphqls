type Track {
    id: UUID!
    title: String!
    artist: String!
    length: Int!
    isrcs: [String!]!
    linkedYoutube: Boolean!
    linkedPlex: Boolean!
}

extend type Query {
    getTracks: [Track!]! @auth
}

extend type Mutation {
    deleteTrack(id: UUID!): Boolean! @auth
}